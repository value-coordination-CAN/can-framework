"""Agent participation: steward-bound identities, derived records and throughput limits.

Implements the proposals in docs/about/a-note-from-an-ai-contributor.md:
agents derive rather than witness, every agent answers to a named steward, derived
records are reproducible and superseded when recomputation disagrees, writes pause
when the unreviewed queue is full, and agents hold mandates but never entitlements.
"""
