# Minecraft connection
MC_HOST=localhost
MC_PORT=25565
MC_USERNAME=can_bot_01
MC_VERSION=1.20.1

# CAN backend
CAN_BASE_URL=http://localhost:8000
# Bearer token for CAN backend (OIDC or DID-session token)
CAN_BEARER_TOKEN=REPLACE_ME
# Which CAN user_id to attribute bot events to
CAN_USER_ID=REPLACE_ME

# Optional: auto-score after N events
AUTO_SCORE_EVERY=10
